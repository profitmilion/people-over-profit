import React from "react";
import { Link } from "react-router-dom";

/**
 * Prosta sekcja pod DEMO:
 * - informacja, że istnieje pełne archiwum
 * - link przekierowujący do /archive
 *
 * Bez listy, bez demo, bez liczenia cykli.
 */
const WinnersArchive: React.FC = () => {
  return (
    <div> POP33 by ProifitMilion  </div>
  );
};

export default WinnersArchive;

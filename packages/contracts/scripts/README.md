# Scripts

Deployment is intentionally not implemented in the current stage. Future
scripts must consume explicit network configuration and must never replace the
frontend contract address implicitly.
